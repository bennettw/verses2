Next:
  x add the ability to add verses from the passage screen in the passage edit view
    x can't get the ui to save the user_refernece field--deleted accessors
  x clean up the ui 

x Add login/auth
Clean up flash messages, they currently have no style; make header navbar smaller, incl username
-> Restrict passages to the logged in user
  Somehow detect if a user is logged in go straight to the passages page, and if not then login page
Update the user controller to use usernames instead of ids
Then load up a bunch of data
Enable sorting by verse or date
Make esv explicit
Add searching
Add settings page
Add landing/splash page
Register domain/launch
